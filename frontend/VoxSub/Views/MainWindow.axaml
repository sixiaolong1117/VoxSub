<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:VoxSub.ViewModels"
        xmlns:services="using:VoxSub.Services"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DesignWidth="1040"
        d:DesignHeight="720"
        x:Class="VoxSub.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="{services:Localization Key=WindowTitle}"
        Width="1040"
        Height="720"
        MinWidth="900"
        MinHeight="620"
        Background="Transparent"
        TransparencyLevelHint="Mica,Blur">

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    <Grid RowDefinitions="48,Auto,*">

        <Border Grid.Row="0" Classes="titleBar">
            <Grid ColumnDefinitions="Auto,*,Auto"
                  Margin="16,0"
                  VerticalAlignment="Center">

                <StackPanel Orientation="Horizontal"
                            Spacing="10"
                            VerticalAlignment="Center">
                    <TextBlock Text="VoxSub"
                               FontSize="15"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center" />
                    <TextBlock Text="{services:Localization Key=WindowSubtitle}"
                               Classes="statusText"
                               VerticalAlignment="Center" />
                </StackPanel>

                <Border Grid.Column="1"
                        Classes="statusPill"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="{services:Localization Key=StatusLabel}"
                                   Classes="statusText"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="{Binding StatusText}"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Border>

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            Spacing="6"
                            VerticalAlignment="Center">
                    <Button Content="{services:Localization Key=Start}"
                            Command="{Binding RunCommand}"
                            IsEnabled="{Binding CanEdit}"
                            Classes="accent"
                            MinWidth="82" />
                    <Button Content="{services:Localization Key=Cancel}"
                            Command="{Binding CancelCommand}"
                            MinWidth="82" />
                    <Button Content="{services:Localization Key=ClearLog}"
                            Command="{Binding ClearLogCommand}"
                            MinWidth="88" />
                    <Button Content="{services:Localization Key=Settings}"
                            Click="OpenSettings_Click"
                            MinWidth="74" />
                    <Button Content="{services:Localization Key=AboutTitle}"
                            Click="OpenAbout_Click"
                            MinWidth="74" />
                </StackPanel>
            </Grid>
        </Border>

        <ProgressBar Grid.Row="1"
                     IsIndeterminate="{Binding IsRunning}"
                     IsVisible="{Binding IsRunning}"
                     Height="3" />

        <Grid Grid.Row="2"
              ColumnDefinitions="1.12*,*"
              ColumnSpacing="12"
              Margin="16,12,16,16">

            <ScrollViewer Grid.Column="0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <StackPanel Spacing="12">

                    <Border Classes="settingsCard">
                        <StackPanel Spacing="10">
                            <TextBlock Text="{services:Localization Key=CommandSection}" Classes="sectionTitle" />
                            <WrapPanel IsEnabled="{Binding CanEdit}">
                                <RadioButton Content="{services:Localization Key=TranscribeSubtitle}"
                                             IsChecked="{Binding IsTranscribeSelected}"
                                             Margin="0,0,16,6" />
                                <RadioButton Content="{services:Localization Key=EmbedSubtitle}"
                                             IsChecked="{Binding IsEmbedSelected}"
                                             Margin="0,0,16,6" />
                                <RadioButton Content="{services:Localization Key=TranscribeAndEmbed}"
                                             IsChecked="{Binding IsAllSelected}"
                                             Margin="0,0,16,6" />
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Border Classes="settingsCard">
                        <StackPanel Spacing="10">
                            <TextBlock Text="{services:Localization Key=FileSection}" Classes="sectionTitle" />

                            <Grid ColumnDefinitions="96,16,*,Auto"
                                  ColumnSpacing="6"
                                  IsEnabled="{Binding CanEdit}">
                                <TextBlock Grid.Column="0"
                                           Text="{services:Localization Key=MediaFile}"
                                           Classes="label" />
                                <TextBlock Grid.Column="1"
                                           Text="*"
                                           Classes="required"
                                           VerticalAlignment="Center" />
                                <TextBox Grid.Column="2"
                                         Text="{Binding MediaPath}"
                                         Watermark="{services:Localization Key=MediaFilePlaceholder}" />
                                <Button Grid.Column="3"
                                        Content="{services:Localization Key=Browse}"
                                        Command="{Binding BrowseMediaCommand}"
                                        MinWidth="74" />
                            </Grid>

                            <Grid ColumnDefinitions="96,16,*,Auto"
                                  ColumnSpacing="6"
                                  IsVisible="{Binding IsEmbedMode}"
                                  IsEnabled="{Binding CanEdit}">
                                <TextBlock Grid.Column="0"
                                           Text="{services:Localization Key=SubtitleFile}"
                                           Classes="label" />
                                <TextBlock Grid.Column="1" />
                                <TextBox Grid.Column="2"
                                         Text="{Binding SubtitlePath}"
                                         Watermark="{services:Localization Key=SubtitleFilePlaceholder}" />
                                <Button Grid.Column="3"
                                        Content="{services:Localization Key=Browse}"
                                        Command="{Binding BrowseSubtitleCommand}"
                                        MinWidth="74" />
                            </Grid>

                            <Grid ColumnDefinitions="96,16,*,Auto"
                                  ColumnSpacing="6"
                                  IsVisible="{Binding IsOutputSrtEnabled}"
                                  IsEnabled="{Binding CanEdit}">
                                <TextBlock Grid.Column="0"
                                           Text="{services:Localization Key=SrtOutput}"
                                           Classes="label" />
                                <TextBlock Grid.Column="1" />
                                <TextBox Grid.Column="2"
                                         Text="{Binding OutputSrtPath}"
                                         Watermark="{services:Localization Key=SrtOutputPlaceholder}" />
                                <Button Grid.Column="3"
                                        Content="{services:Localization Key=Browse}"
                                        Command="{Binding BrowseOutputSrtCommand}"
                                        MinWidth="74" />
                            </Grid>

                            <Grid ColumnDefinitions="96,16,*,Auto"
                                  ColumnSpacing="6"
                                  IsVisible="{Binding IsOutputVideoEnabled}"
                                  IsEnabled="{Binding CanEdit}">
                                <TextBlock Grid.Column="0"
                                           Text="{services:Localization Key=MkvOutput}"
                                           Classes="label" />
                                <TextBlock Grid.Column="1" />
                                <TextBox Grid.Column="2"
                                         Text="{Binding OutputVideoPath}"
                                         Watermark="{services:Localization Key=MkvOutputPlaceholder}" />
                                <Button Grid.Column="3"
                                        Content="{services:Localization Key=Browse}"
                                        Command="{Binding BrowseOutputVideoCommand}"
                                        MinWidth="74" />
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Classes="settingsCard">
                        <Grid RowDefinitions="Auto,Auto,Auto,Auto"
                              RowSpacing="10">
                            <TextBlock Grid.Row="0"
                                       Text="{services:Localization Key=ParametersSection}"
                                       Classes="sectionTitle"
                                       Margin="0,0,0,2" />

                            <Grid Grid.Row="1"
                                  ColumnDefinitions="96,*"
                                  ColumnSpacing="6"
                                  IsEnabled="{Binding CanEdit}">
                                <TextBlock Grid.Column="0"
                                           Text="{services:Localization Key=Language}"
                                           Classes="label" />
                                <ComboBox Grid.Column="1"
                                          ItemsSource="{Binding ActiveLanguageOptions}"
                                          Text="{Binding Language}"
                                          IsEditable="True"
                                          HorizontalAlignment="Stretch" />
                            </Grid>

                            <Grid Grid.Row="2"
                                  RowDefinitions="Auto,Auto"
                                  ColumnDefinitions="96,*,80,*"
                                  RowSpacing="10"
                                  ColumnSpacing="6"
                                  IsEnabled="{Binding CanEdit}"
                                  IsVisible="{Binding IsWhisperParamsVisible}">
                                <TextBlock Grid.Row="0"
                                           Grid.Column="0"
                                           Text="{services:Localization Key=Model}"
                                           Classes="label" />
                                <ComboBox Grid.Row="0"
                                          Grid.Column="1"
                                          ItemsSource="{Binding ModelOptions}"
                                          SelectedItem="{Binding Model}"
                                          HorizontalAlignment="Stretch" />

                                <TextBlock Grid.Row="0"
                                           Grid.Column="2"
                                           Text="{services:Localization Key=Device}"
                                           Classes="label" />
                                <ComboBox Grid.Row="0"
                                          Grid.Column="3"
                                          ItemsSource="{Binding DeviceOptions}"
                                          SelectedItem="{Binding Device}"
                                          HorizontalAlignment="Stretch" />

                                <TextBlock Grid.Row="1"
                                           Grid.Column="0"
                                           Text="{services:Localization Key=Fp16}"
                                           Classes="label" />
                                <ComboBox Grid.Row="1"
                                          Grid.Column="1"
                                          ItemsSource="{Binding Fp16Options}"
                                          SelectedItem="{Binding Fp16}"
                                          HorizontalAlignment="Stretch" />
                            </Grid>

                            <WrapPanel Grid.Row="3"
                                       IsEnabled="{Binding CanEdit}">
                                <CheckBox Content="{services:Localization Key=Verbose}"
                                          IsChecked="{Binding Verbose}"
                                          IsVisible="{Binding IsWhisperParamsVisible}"
                                          Margin="0,0,18,0" />
                                <CheckBox Content="{services:Localization Key=Overwrite}"
                                          IsChecked="{Binding Overwrite}"
                                          IsVisible="{Binding IsEmbedParamsVisible}"
                                          Margin="0,0,18,0" />
                            </WrapPanel>
                        </Grid>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <Border Grid.Column="1"
                    Classes="settingsCard"
                    Padding="0">
                <Grid RowDefinitions="Auto,*" Margin="16">
                    <Grid ColumnDefinitions="*,Auto,Auto" Margin="0,0,0,8">
                        <TextBlock Text="{services:Localization Key=LogOutputSection}"
                                   Classes="sectionTitle"
                                   Margin="0" />
                        <CheckBox x:Name="FollowLogCheckBox"
                                  Grid.Column="1"
                                  Content="{services:Localization Key=FollowLog}"
                                  IsChecked="True"
                                  VerticalAlignment="Center"
                                  Margin="0,0,12,0" />
                        <TextBlock Grid.Column="2"
                                   Text="{Binding StatusText}"
                                   Classes="statusText"
                                   VerticalAlignment="Center" />
                    </Grid>

                    <ScrollViewer x:Name="LogScrollViewer"
                                  Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  Margin="-16,0"
                                  Padding="16,0">
                        <TextBox x:Name="LogTextBox"
                                 Text="{Binding LogText}"
                                 Classes="mono borderless"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Stretch"
                                 MinHeight="420"
                                 TextChanged="LogTextBox_TextChanged" />
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
